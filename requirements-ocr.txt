# Optional GPU extras:
#   * Unlimited-OCR document parsing (https://github.com/baidu/Unlimited-OCR)
#   * faster-whisper audio/video transcription (CUDA via CTranslate2; the
#     cuBLAS/cuDNN DLLs it needs ship inside the torch CUDA wheel below)
# Requires an NVIDIA GPU with >= 8 GB VRAM. Pins follow the upstream README
# (tested there on Python 3.12 + CUDA 12.9). The +cu128 torch build supports
# Blackwell (RTX 50-series) GPUs; it is NOT the CPU-only wheel from PyPI.
#
#   venv\Scripts\python -m pip install -r requirements-ocr.txt
#
faster-whisper>=1.1
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
torchvision==0.25.0+cu128
transformers==4.57.1
pymupdf==1.27.2.2
Pillow>=12.0
matplotlib>=3.10
einops==0.8.2
addict==2.4.0
easydict==1.13
psutil>=7.0
tqdm
numpy
accelerate
